BEGIN
  FOR L_INDEX_ROW IN (SELECT UI.INDEX_NAME
                        FROM USER_INDEXES UI
                       WHERE UI.TABLE_NAME = '&1')
  LOOP
    EXECUTE IMMEDIATE 'ALTER INDEX ' || L_INDEX_ROW.INDEX_NAME || ' REBUILD';
  END LOOP;
END;
/